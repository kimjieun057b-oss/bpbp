export default function CategoryBanner () {
    // 1. main banner (home icon > menu > sub menu)
    // 2. main banner (home | menu▽ | sub menu▽ )
    // 3. main banner (menu, contents) | left sidebar menu
    // 4. main banner (menu, contents) | tab menu (sub menu)
    return (
        <div>

        </div>
    )
}